# FastAPI backend URL (default: http://localhost:8000)
VITE_API_URL=http://localhost:8000

# Cloudinary cloud name for event hero images.
# When set, images are loaded via Cloudinary CDN.
# When absent, a local placeholder SVG is used instead.
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name_here
