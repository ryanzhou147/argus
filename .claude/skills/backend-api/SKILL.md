# Backend API

Use this skill when working on the FastAPI backend.

Rules:

- Use FastAPI routers, schemas, services, and repositories
- Keep seeded fake data behind a repository abstraction
- Return typed response models
- Match the project API routes exactly
- No mutation routes in v1
- No auth
- Keep future AWS PostgreSQL integration easy
