# Seed Data

Use this skill when generating or updating fake data.

Rules:

- Match the real project tables exactly
- Use realistic global event examples
- Ensure every event has:
  - coordinates
  - event type
  - start/end times
  - canada impact summary
  - related events
  - top source content
- Include Cloudinary-compatible image references for demo rendering
